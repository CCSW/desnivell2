@model IList<ManufacturerModel>
@using Nop.Core;
@using Nop.Core.Infrastructure;
@using Nop.Web.Framework.UI;
@using Nop.Web.Models.Catalog;
<div class="manufacturerlist-page">
    <div class="page-title">
        <h1>@T("Manufacturers.List")</h1>
    </div>
    <div class="clear">
    </div>
   @*<div class="manufacturer-grid">
        @if (Model.Count > 0)
        {
            @(Html.DataList<ManufacturerModel>(Model, 3,
                    @<div class="manufacturer-item">
                        <h2 class="man-title">
                            <a href="@Url.RouteUrl("Manufacturer", new { manufacturerId = item.Id, SeName = item.SeName })" title="@item.PictureModel.Title">
                                @item.Name</a>
                        </h2>
                        <div class="picture">
                            <a href="@Url.RouteUrl("Manufacturer", new { manufacturerId = item.Id, SeName = item.SeName })" title="@item.PictureModel.Title">
                                <img alt="@item.PictureModel.AlternateText" src="@item.PictureModel.ImageUrl" title="@item.PictureModel.Title" /></a>
                        </div>
                    </div>
                ))
        }
    </div>*@

@{    
    //resources
    Html.AppendCssFileParts(@Url.Content("~/Themes/Desnivell/Content/jcarouse_horizontal.css"));
    Html.AppendScriptParts(@Url.Content("~/Scripts/jquery-1.7.1.min.js"));
    Html.AppendScriptParts(@Url.Content("~/Scripts/jquery.jcarousel-all.min.js"));    
}
<script type="text/javascript">
    $(document).ready(function () {
        var marcasCarousel = $('#marcasCarousel').jcarousel({
            // Core configuration goes here
            wrap: 'circular'
        });

//        $('#marcasCarousel-pagination').jcarouselPagination({
//            // Configuration options
//            carousel: marcasCarousel
//        });

        $('#marcas-prev').jcarouselControl({
            target: '-=2',
            carousel: marcasCarousel
        });

        $('#marcas-next').jcarouselControl({
            target: '+=2',
            carousel: marcasCarousel
        });
    });
</script>
   @if (Model.Count > 0)
    {
    <div class="jcarousel-skin-default">
        <div class="jcarousel" id="marcasCarousel">
        <ul>
        @foreach (var item in Model)
        {
            <li style="vertical-align:middle">
                <a href="@Url.RouteUrl("Manufacturer", new { manufacturerId = item.Id, SeName = item.SeName })" title="@item.PictureModel.Title">
                   <img style="vertical-align:middle !important"@* width="100" height="40"*@ alt="@item.PictureModel.AlternateText" src="@item.PictureModel.ImageUrl" title="@item.PictureModel.Title"/>
               </a>
            </li>       
        }
        </ul>
        </div>
        <div class="jcarousel-controls">
            <!-- Controls -->
            <a id="marcas-prev" class="jcarousel-prev" href="#">Prev</a>
            <a id="marcas-next" class="jcarousel-next" href="#">Next</a>
        </div>
        <!-- Pagination
        <div id="marcasCarousel-pagination" class="jcarousel-pagination"></div>
        -->
        </div>
    }
</div>
