@model IList<CategoryModel>

@using Nop.Web.Framework.UI;
@using Nop.Web.Models.Catalog;
@*
<script type="text/javascript">
    jQuery(document).ready(function () {
        jQuery('#mycarouselTop').jcarousel({
            wrap: 'circular',
            visible: 1,
            scroll: 1,
            auto: 5
        });
    });
</script>
<div id="wrap">
    <ul id="mycarouselTop" class="jcarousel-skin-tango jcarousel-banner">
        <li><img height="auto" width="500" alt="" src="/Content/Images/uploaded/nivo/scott.png" title=""/></li>       
        <li><img height="auto" width="500" alt="" src="/Content/Images/uploaded/nivo/homeroad2_0.png" title=""/></li>       
    </ul>
</div>
*@
@if (Model.Count > 0)
{
    <div class="home-page-category-grid">
        @(Html.DataList<CategoryModel>(Model, 3,
                @<div class="item-box">
                    <div class="category-item">
                        <h2 class="title">
                            <a href="@Url.RouteUrl("Category", new { categoryId = item.Id, SeName = item.SeName })" title="@item.PictureModel.Title">
                                @item.Name</a>
                        </h2>
                        <div class="picture">
                            <a href="@Url.RouteUrl("Category", new { categoryId = item.Id, SeName = item.SeName })" title="@item.PictureModel.Title">
                                <img alt="@item.PictureModel.AlternateText" src="@item.PictureModel.ImageUrl"
                                    title="@item.PictureModel.Title" /></a>
                        </div>
                    </div>
                </div>
            ))
    </div>
}